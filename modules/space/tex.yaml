
styles:
    space-tex:
        texcoords: true
        shaders:
            blocks:
                global: |   
                    // Get the position on TexCoords and
                    // the Z of the World Position
                    // ================================
                    vec2 getTexCoords () {
                        return v_texcoord.xy;
                    }