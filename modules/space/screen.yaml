
styles:
    space-screen:
        shaders:
            blocks:
                global: |
                    // Get the coordinates in screen space
                    // ================================
                    vec2 getScreenCoords () {
                        return gl_FragCoord.xy / u_resolution.xy;
                    }